# CLAUDE.md

Claude Code reads this file; every other agent reads `AGENTS.md`. So the
instructions live there and this file only imports them. **Do not fork a rule
into here** — one list, or the two disagree and nobody notices which is stale.

@AGENTS.md

Two more, for the toolchain you are in: `pipeline/AGENTS.md` (Python ingest) and
`site/AGENTS.md` (the Svelte SPA).

Before writing a commit message, a PR body, a comment or a doc, read
`docs/writing-style.md`. `AGENTS.md` §Writing has the short form.
