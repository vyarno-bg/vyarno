# Funding for Вярно (vyarno.bg).
#
# Вярно is a public good: every feature is free to everyone, there is no
# paid version and nothing is held back. Donations cover the domain and the
# server. They buy no feature, no priority and no influence over any figure
# the site publishes.
#
# KEEP THIS FILE IN STEP WITH `site/src/lib/support.js`. That module is what
# the site itself renders, and it carries a `live` flag per platform so a
# donate link never points at an account that does not exist yet.
# `site/scripts/verify_support.mjs` fails the build if the two disagree.
#
# A platform's line is uncommented only while its account exists. Opening a
# channel is three steps, in this order:
#   1. open the account on the platform;
#   2. uncomment the line here;
#   3. flip `live: true` for that platform in `site/src/lib/support.js`.
# Doing 2 without 3 (or the reverse) is what the verify script catches.
#
# GitHub Sponsors costs more than three steps: the sponsors account has to
# belong to `REPO_OWNER` in `site/src/lib/legal-nav.js`, which
# `verify_legal.mjs` checks, so a sponsors account under any other name is a
# red test rather than a Sponsor button pointing somewhere the project does
# not control.
#
# A channel is also a processor of a donor's name, e-mail and message, so the
# privacy notice has to name it — `verify_support.mjs` fails if a live
# platform is missing from `legal.js`'s notice, and `LEGAL_VERSION` moves in
# the same release. That is step 4, and it is the one nothing about opening an
# account reminds anybody of.

github: [vyarno-bg]
ko_fi: vyarno

# open_collective: vyarno
# liberapay: vyarno
