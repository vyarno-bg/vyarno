// Shared Svelte config. Vite reads it through @sveltejs/vite-plugin-svelte and
// eslint-plugin-svelte reads it to resolve component syntax the same way the
// build does, so the linter and the bundler cannot disagree about the source.
export default {
  compilerOptions: {},
};
